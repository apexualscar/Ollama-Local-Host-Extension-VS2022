<UserControl x:Class="OllamaLocalHostIntergration.Controls.ContextChipControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
             toolkit:Themes.UseVsTheme="True">
    <!-- Phase 6.3: Fully rounded chip, no square container -->
    <Border Background="{DynamicResource {x:Static vsshell:VsBrushes.AccentPaleKey}}"
            CornerRadius="12"
            Padding="8,4"
            Margin="0,0,4,4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Icon -->
            <TextBlock Grid.Column="0"
                       Text="{Binding Icon}"
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="10"
                       Margin="0,0,4,0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.AccentMediumKey}}"/>
            
            <!-- Display Text -->
            <TextBlock Grid.Column="1"
                       Text="{Binding DisplayText}"
                       FontSize="10"
                       FontWeight="SemiBold"
                       MaxWidth="150"
                       TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.AccentMediumKey}}"/>
            
            <!-- Token Count -->
            <TextBlock Grid.Column="2"
                       FontSize="9"
                       Opacity="0.7"
                       Margin="4,0,4,0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.AccentMediumKey}}">
                <Run Text="~"/><Run Text="{Binding TokenCount}"/> tokens
            </TextBlock>
            
            <!-- Remove Button -->
            <Button Grid.Column="3"
                    Content="?"
                    FontSize="10"
                    Width="16"
                    Height="16"
                    Padding="0"
                    Margin="4,0,0,0"
                    VerticalAlignment="Center"
                    ToolTip="Remove context"
                    Click="RemoveClick"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{DynamicResource {x:Static vsshell:VsBrushes.AccentMediumKey}}"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.AccentMediumKey}}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>
